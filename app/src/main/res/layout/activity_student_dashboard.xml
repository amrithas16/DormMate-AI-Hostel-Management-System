<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:text="Student Dashboard"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_gravity="center"
            android:layout_marginBottom="20dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <Button android:id="@+id/btnProfile" android:text="Profile"/>
        <Button android:id="@+id/btnRoom" android:text="Room Details"/>
        <Button android:id="@+id/btnLeave" android:text="Apply Leave"/>
        <Button android:id="@+id/btnComplaint" android:text="Complaints"/>
        <Button android:id="@+id/btnRules" android:text="Rules & Regulations"/>
        <Button android:id="@+id/btnEmergency" android:text="Emergency Alert"/>
        <Button android:id="@+id/btnLogout" android:text="Logout"/>

    </LinearLayout>
</ScrollView>
