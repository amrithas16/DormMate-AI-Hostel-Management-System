<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:text="Warden Dashboard"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_gravity="center"
            android:layout_marginBottom="20dp"/>

        <Button android:id="@+id/btnLeaveReq" android:text="Leave Requests"/>
        <Button android:id="@+id/btnComplaints" android:text="Complaints"/>
        <Button android:id="@+id/btnAnnouncement" android:text="Send Announcement"/>
        <Button android:id="@+id/btnLogs" android:text="Student Entry Logs"/>
        <Button android:id="@+id/btnEmergency" android:text="Emergency Alerts"/>
        <Button android:id="@+id/btnLogout" android:text="Logout"/>

    </LinearLayout>
</ScrollView>
