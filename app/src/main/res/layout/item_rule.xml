<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_glass_card"
    android:orientation="horizontal"
    android:padding="16dp"
    android:layout_marginBottom="10dp">

    <TextView android:id="@+id/tvRuleNumber"
        android:layout_width="32dp" android:layout_height="32dp"
        android:gravity="center" android:text="1"
        android:textColor="@color/electric_violet" android:textStyle="bold"
        android:textSize="16sp"/>

    <TextView android:id="@+id/tvRuleText"
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_weight="1" android:text="Rule text"
        android:textColor="#CCFFFFFF" android:textSize="14sp"
        android:layout_marginStart="12dp" android:layout_gravity="center_vertical"/>
</LinearLayout>
